## AI Illuminate Static

- Used PHP just because that's what the main website is. PHP is only used for including files though to keep the static dev neat.
- Images / CSS / JS are in the `build` folder. You should only need to include main.css and main.js, nothing else related to Frontend.